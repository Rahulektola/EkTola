{
  "name": "ektola-frontend",
  "private": true,
  "version": "0.1.0",
  "description": "ekTola frontend (TypeScript migration)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
