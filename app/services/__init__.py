"""
Services package — consolidated business logic, routes, and background tasks.

Naming convention:
  *_routes.py   — FastAPI router modules (HTTP endpoints)
  *_tasks.py    — Celery background task modules
  *_service.py  — Core business logic / external API wrappers
"""
